#!/usr/bin/env php
<?php
if (!$loader = include __DIR__ . '/../../../autoload.php') {
    die('You must set up the project dependencies.');
}

$input = new \Prim\Console\Input($argv);

$console = new \Prim\Console\Console([], $input);
$console->run();